# Copia este archivo como .env (sin la extensión .example) y completa los valores antes de ejecutar la API.

# MongoDB
MongoDbSettings__ConnectionString=mongodb+srv://<usuario>:<password>@<cluster-host>/?retryWrites=true&w=majority
MongoDbSettings__DatabaseName=<nombre-base-datos>

# JWT
JwtSettings__SecretKey=<llave-de-al-menos-32-caracteres>
JwtSettings__Issuer=<emisor>
JwtSettings__Audience=<audiencia>
JwtSettings__ExpirationMinutes=120

# ASP.NET Core
ASPNETCORE_ENVIRONMENT=Development
